# 后端依赖已迁移至 pyproject.toml，请使用 uv 安装：
#
#   cd backend
#   uv sync --group dev
#
# pip 兼容（Linux/macOS）：
#   pip install -r requirements
#
# pip 兼容（Windows）：
#   pip install -r requirements-windows.txt
